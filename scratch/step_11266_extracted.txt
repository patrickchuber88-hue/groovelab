"                {rooms.map((room) => {\n                  const isSelected = selectedCampusRoomId === room.id;\n                  \n                  // Live-status check\n                  const now = new Date();\n                  const localDateStr = now.getFullYear() + '-' + String(now.getMonth() + 1).padStart(2, '0') + '-' + String(now.getDate()).padStart(2, '0');\n                  const localMin = now.getHours() * 60 + now.getMinutes();\n\n                  const isOccupiedByManual = campusBookings.some((b: any) => {\n                    if (b.roomId !== room.id || b.date !== localDateStr) return false;\n                    const [shStr, smStr] = b.startTime.split(':');\n                    const [ehStr, emStr] = b.endTime.split(':');\n                    const bStart = (parseInt(shStr) || 0) * 60 + (parseInt(smStr) || 0);\n                    const bEnd = (parseInt(ehStr) || 0) * 60 + (parseInt(emStr) || 0);\n                    return localMin >= bStart && localMin < bEnd;\n                  });\n\n                  const dayOfWeek = now.getDay();\n                  const scheduleDayNum = dayOfWeek === 0 ? 7 : dayOfWeek;\n                  const isOccupiedBySchedule = schedules.some((s: any) => {\n                    if (s.room_id !== room.id || s.day_of_week !== scheduleDayNum) return false;\n                    const [shStr, smStr] = s.time_slot.split(':');\n                    const sStart = (parseInt(shStr) || 0) * 60 + (parseInt(smStr) || 0);\n                    const duration = s.duration || 30;\n                    const sEnd = sStart + duration;\n                    return localMin >= sStart && localMin < sEnd;\n                  });\n\n                  const isCurrentlyOccupied = isOccupiedByManual || isOccupiedBySchedule;\n\n                  return (\n                    <div\n                      key={room.id}\n                      onClick={() => setSelectedCampusRoomId(room.id)}\n                      style={{\n                        minWidth: '180px',\n                        padding:
<truncated 2168 bytes>
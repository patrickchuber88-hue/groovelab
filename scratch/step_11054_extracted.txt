"                                let accentColor = isOwnBooking ? brandColor : '#64748b';\n                                let isDashed = false;\n                                \n                                if (isSchedule) {\n                                  if (b.status === 'approved') {\n                                    accentColor = brandColor; // Campus Green\n                                  } else {\n                                    accentColor = '#d97706';\n                                    isDashed = true;\n                                  }\n                                }\n\n                                // Get premium matching text colors with high accessibility contrast\n                                let primaryColor = accentColor;\n                                let secondaryColor = '#1e293b';\n\n                                if (accentColor === brandColor || accentColor === '#16a34a') {\n                                  primaryColor = '#15803d'; // Rich forest green for text\n                                  secondaryColor = '#14532d'; // Deep pine green for name\n                                } else if (accentColor === '#0284c7') {\n                                  primaryColor = '#0369a1';\n                                  secondaryColor = '#0c4a6e';\n                                } else if (accentColor === '#d97706') {\n                                  primaryColor = '#b45309';\n                                  secondaryColor = '#78350f';\n                                } else {\n                                  primaryColor = '#475569';\n                                  secondaryColor = '#1e293b';\n                                }\n\n                                const bgGradient = `linear-gradient(135deg, ${accentColor}12 0%, ${accentColor}06 100%)`;\n                                const cardBorder = `1px ${isDashed ? 'dashed' : 'solid'} ${accentColor}25`;\n\n                                // Calculate topOffset and height for continuous layout\n      
<truncated 6332 bytes>
"                              onClick={() => {\n                                // Calculate target date based on selected week and dayIdx\n                                const currentSelectedDate = new Date(bookingDate);\n                                const dayOfWeek = currentSelectedDate.getDay();\n                                const diffToMon = currentSelectedDate.getDate() - (dayOfWeek === 0 ? 6 : dayOfWeek - 1);\n                                const targetDate = new Date(currentSelectedDate.setDate(diffToMon + dayIdx));\n                                const dateStr = targetDate.getFullYear() + '-' + String(targetDate.getMonth() + 1).padStart(2, '0') + '-' + String(targetDate.getDate()).padStart(2, '0');\n                                \n                                setBookingDate(dateStr);\n                                setBookingStartTime(hour);\n                                \n                                // Set end time to start hour + 1 hour\n                                const nextHour = slotHour + 1;\n                                const nextHourStr = `${String(nextHour).padStart(2, '0')}:00`;\n                                setBookingEndTime(nextHourStr);\n\n                                // Check if room is available\n                                const targetRoomId = selectedRoom?.id || rooms[0]?.id || '';\n                                const isManualCollision = campusBookings.some((b: any) => {\n                                  if (b.roomId !== targetRoomId || b.date !== dateStr) return false;\n                                  return !(nextHourStr <= b.startTime || hour >= b.endTime);\n                                });\n\n                                const dayOfWeekIndex = targetDate.getDay();\n                                const scheduleDayNum = dayOfWeekIndex === 0 ? 7 : dayOfWeekIndex;\n                                const isScheduleCollision = schedules.some((s: any) => {\n                                  if (s.room_id !== targetRoomId || s.day_of_week !==
<truncated 2099 bytes>
"# Walkthrough - Neue Campus Menu Boards: Unterrichtsfächer & Kooperationen\n\nI have successfully created and integrated the two new menu boards for the Secretary Dashboard under the **Campus** tab: **Unterrichtsfächer** and **Kooperationen**.\n\n## 1. Database Schema\n- Created the tables `public.subjects` and `public.cooperations` in the Supabase PostgreSQL database.\n- Enabled appropriate fields (name, description, category, contact person, email, phone, status, and active states).\n- Disabled RLS for seamless client-side querying.\n\n## 2. Secretary Dashboard Navigation & Layout\n- Added the subtabs `\"subjects\"` and `\"cooperations\"` to the `campusSubTab` state.\n- Registered both new boards in the sidebar array under the Campus active tab, mapping icons (using `BookOpen` for Unterrichtsfächer and `Users` for Kooperationen).\n- Added cases to the `getTabTitle` function to return `'Unterrichtsfächer'` and `'Kooperationen'` as titles.\n\n## 3. Board Component Views\n- Implemented and mounted the interactive card boards `renderSubjectsBoard()` and `renderCooperationsBoard()` in the main Campus dashboard.\n- Integrated search functionality and filter dropdowns (category filter for subjects, status filter for cooperations).\n- Added quick toggles for the active state and full deletion functionality mapped directly to Supabase.\n\n## 4. Bulk Onboarding (Sammel-Onboarding) for Subjects\n- Implemented bulk onboarding (`isSubjectCsvExpanded`, `subjectCsvText`) for Unterrichtsfächer.\n- Parses lines by semi-colons or commas: `Name; Beschreibung (optional); Kategorie (optional)`.\n- If no category is provided, it automatically inherits the active sidebar filter category.\n- Created `handleImportSubjects` handler matching the teacher bulk import logic.\n\n## 5. Right Sidebar (Kategorien) for Subjects\n- Evaluated and implemented a premium **Kategorien** (Categories) sidebar on the right of the Unterrichtsfächer board.\n- Shows all active categories and their subject counts.\n- Allows filtering the list instantl
<truncated 195 bytes>
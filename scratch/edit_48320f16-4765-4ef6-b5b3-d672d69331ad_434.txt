"  const getDaysUntilNextLesson = () => {\n    if (!upcomingLessons || upcomingLessons.length === 0) return null;\n    const nextL = upcomingLessons[0];\n    let targetDate: Date;\n    if (nextL.date instanceof Date) {\n      targetDate = nextL.date;\n    } else if (nextL.dateStr) {\n      const parts = nextL.dateStr.split('.');\n      if (parts.length === 3) {\n        targetDate = new Date(parseInt(parts[2]), parseInt(parts[1]) - 1, parseInt(parts[0]));\n      } else {\n        targetDate = new Date();\n      }\n    } else {\n      return null;\n    }\n    \n    const today = new Date();\n    today.setHours(0, 0, 0, 0);\n    targetDate.setHours(0, 0, 0, 0);\n    const diffTime = targetDate.getTime() - today.getTime();\n    const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));\n    return diffDays;\n  };\n\n  const daysUntilLesson = getDaysUntilNextLesson();\n  const allCompleted = dailyChecklist.every(t => t.completed);\n  const studentName = student?.nickname || student?.first_name || 'Champion';\n\n  // Filtered Song items\n  const filteredSongs = progressItems.filter(item => {\n    const matchesSearch = item.topic_name.toLowerCase().includes(songSearch.toLowerCase());"
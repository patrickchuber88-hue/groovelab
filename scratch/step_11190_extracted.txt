"    const handleAddBooking = (roomId: string) => {\n      // 1. Parse proposed booking times\n      const [pShStr, pSmStr] = bookingStartTime.split(':');\n      const pSh = parseInt(pShStr) || 0;\n      const pSm = parseInt(pSmStr) || 0;\n      const pStartMin = pSh * 60 + pSm;\n\n      const [pEhStr, pEmStr] = bookingEndTime.split(':');\n      const pEh = parseInt(pEhStr) || 0;\n      const pEm = parseInt(pEmStr) || 0;\n      const pEndMin = pEh * 60 + pEm;\n\n      if (pEndMin <= pStartMin) {\n        alert('Ungültige Uhrzeit: Die Endzeit muss nach der Startzeit liegen.');\n        return;\n      }\n\n      // 2. Check collision with manual bookings in the same room on this date\n      const roomManualBookings = campusBookings.filter((b: any) => b.roomId === roomId && b.date === bookingDate);\n      for (const b of roomManualBookings) {\n        const [bShStr, bSmStr] = b.startTime.split(':');\n        const bSh = parseInt(bShStr) || 0;\n        const bSm = parseInt(bSmStr) || 0;\n        const bStartMin = bSh * 60 + bSm;\n\n        const [bEhStr, bEmStr] = b.endTime.split(':');\n        const bEh = parseInt(bEhStr) || 0;\n        const bEm = parseInt(bEmStr) || 0;\n        const bEndMin = bEh * 60 + bEm;\n\n        if (pStartMin < bEndMin && pEndMin > bStartMin) {\n          alert(`Raum belegt: In diesem Raum existiert bereits eine Raumbuchung von ${b.startTime} bis ${b.endTime} Uhr (${b.purpose}).`);\n          return;\n        }\n      }\n\n      // 3. Check collision with recurring weekly schedules in the same room\n      const d = new Date(bookingDate);\n      const dayOfWeekIndex = d.getDay(); // 0 = Sun, 1 = Mon, ...\n      const scheduleDayNum = dayOfWeekIndex === 0 ? 7 : dayOfWeekIndex; // 1 = Mon, ..., 7 = Sun\n\n      const roomSchedules = schedules.filter((s: any) => s.room_id === roomId && s.day_of_week === scheduleDayNum);\n      for (const s of roomSchedules) {\n        const [sShStr, sSmStr] = s.time_slot.split(':');\n        const sSh = parseInt(sShStr) || 0;\n        const sSm = parseInt(sSm
<truncated 1569 bytes>
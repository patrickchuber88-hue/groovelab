"                                let accentColor = isOwnBooking ? brandColor : '#64748b';\n                                let isDashed = false;\n                                \n                                if (isSchedule) {\n                                  if (b.status === 'approved') {\n                                    accentColor = '#0284c7';\n                                  } else {\n                                    accentColor = '#d97706';\n                                    isDashed = true;\n                                  }\n                                }\n\n                                const bgGradient = `linear-gradient(135deg, ${accentColor}10 0%, ${accentColor}05 100%)`;\n                                const cardBorder = `1px ${isDashed ? 'dashed' : 'solid'} ${accentColor}25`;\n                                const primaryColor = accentColor;\n                                const secondaryColor = isOwnBooking ? `${brandColor}d0` : '#475569';\n\n                                // Calculate topOffset and height for continuous layout\n                                const [shStr, smStr] = b.startTime.split(':');\n                                const sh = parseInt(shStr) || 0;\n                                const sm = parseInt(smStr) || 0;\n\n                                const [ehStr, emStr] = b.endTime.split(':');\n                                const eh = parseInt(ehStr) || 0;\n                                const em = parseInt(emStr) || 0;\n\n                                const gridStartMin = 8 * 60;\n                                const gridEndMin = 22 * 60;\n\n                                const startMin = Math.max(gridStartMin, sh * 60 + sm);\n                                const endMin = Math.min(gridEndMin, eh * 60 + em);\n\n                                const durationMins = endMin - startMin;\n                                if (durationMins <= 0) return null;\n\n                                const slotStartMin = slotHour * 60;\n\n                       
<truncated 6184 bytes>
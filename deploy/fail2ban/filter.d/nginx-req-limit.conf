# ==============================================================================
# Fail2ban Filter for Nginx Rate Limit Exceeded
# Target: /etc/fail2ban/filter.d/nginx-req-limit.conf
# ==============================================================================

[Definition]
failregex = limiting requests, excess: .* by zone ".*", client: <HOST>
ignoreregex =
